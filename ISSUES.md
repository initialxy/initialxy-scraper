in cli.ts is there a way to get version from package.json instead of just hard coding it?


If the input URL does not start with http:// or https:// just prefix it with https:// for convenience.


create a --flat-dir flag such that when given, the dumped files will be put in the --output-dir directory without any subdirectories. If there's a naming conflict, then just let it overwrite


main.ts L241 "Min" should be replaced by app.name


Why does ui-panel.html still have a large blob of Javascript in it. I asked you to extract it as a .ts file and rewrite it with TypeScript type safety. Then you can import generateCurl from utils.ts instead of having to leave it in preload.


tooltip when hovering over rows on Network Monitor doesn't appear.